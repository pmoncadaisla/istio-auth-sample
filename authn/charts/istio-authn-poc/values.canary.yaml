# Default values for api.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 2
environment: canary
autoscaler:
  enabled: true
disruptionbudget:
  enabled: true
ingress:
  # Used to create an Ingress record.
  records:
    - host: istio-authn-canary.lab.digitalhub.masmovil.com
      path: /
resources:
   limits:
    cpu: 100m
    memory: 128Mi
   requests:
    cpu: 10m
    memory: 64Mi
